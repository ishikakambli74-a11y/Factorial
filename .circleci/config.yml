version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run Factorial Program
          command: python factorial.py

workflows:
  build_and_test:
    jobs:
      - build
